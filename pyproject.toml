[project]
name = "cnc-gcode-snipet"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyserial>=3.5",
]
[project.optional-dependencies]
dev = ["pytest", "ruff"]